const Button = (_props) => <button type="button" />

const meta = {
  component: Button,
}

export default meta

export const Primary = {
  // 👇 Rename this story
  name: 'I am the primary',
  args: {
    label: 'Button',
  },
}
