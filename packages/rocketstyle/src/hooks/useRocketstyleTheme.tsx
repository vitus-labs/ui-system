export default undefined
// import { getDimensionThemes } from '~/utils/theme'
// import { getDimensionsMap } from '~/utils/dimensions'
// import type { Configuration, __ROCKETSTYLE__ } from '~/types/configuration'
// import type { ThemeModeCallback } from '~/types/theme'

// type UseTheme = (params: {
//   theme: Record<string, any>
//   options: Configuration
//   cb: ThemeModeCallback
// }) => __ROCKETSTYLE__

// const useTheme: UseTheme = ({ theme, options }) => {
//   const themes = getDimensionThemes(theme, options)

//   const { keysMap, keywords } = getDimensionsMap({
//     themes,
//     useBooleans: options.useBooleans,
//   })

//   // eslint-disable-next-line no-underscore-dangle
//   const __ROCKETSTYLE__ = {
//     dimensions: keysMap,
//     reservedPropNames: keywords,
//   }

//   return __ROCKETSTYLE__
// }

// export default useTheme
