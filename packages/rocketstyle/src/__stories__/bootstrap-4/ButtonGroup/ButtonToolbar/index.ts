import { element } from '../../base'

export default element
  .config({
    name: 'bootstrap-4/ButtonGroup.ButtonToolbar',
  })
  .attrs({
    block: true,
    role: 'toolbar',
    'aria-label': 'Toolbar with button groups',
  })
